# BorderWrapper

This is a reusable component, used as a wrapper for the forms.

```js
import { BorderWrapper } from './components'
```

```tsx
<BorderWrapper logo="the logo" formHeading="Sign In">
  Some content
</BorderWrapper>
```
